# Control Center v2 (WIP)

This repo is built **by** Control Center v1. See [`docs/PLAN.md`](docs/PLAN.md)
for the architecture and [`docs/WORK_PACKETS.md`](docs/WORK_PACKETS.md) for
the ordered task list dispatched to `@gemini` via v1's dashboard.

**v1 is not touched.** It continues to run at `~/.config/control-center/`
and is the orchestrator for this build.

Status: P0 complete (plan + packets). Next: P1.1 dispatch.
